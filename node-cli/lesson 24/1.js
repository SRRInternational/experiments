const alert = require("./index");

alert();

alert({
  type: "success",
  msg: "All done",
});

alert({
  type: "warning",
  msg: "All done",
});

alert({
  type: "info",
  msg: "All done",
});

alert({
  type: "error",
  msg: "All done",
});
