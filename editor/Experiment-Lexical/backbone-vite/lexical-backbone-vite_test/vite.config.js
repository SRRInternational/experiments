import { defineConfig } from 'vite';

/* export default defineConfig({
  resolve: {
    alias: {
      'lexical-react': 'node_modules\\@lexical\\react', // Adjust the path as needed
    },
  },
}); */


/* export default defineConfig({
  optimizeDeps: {
    include: [
      //'@lexical/react',
      // Add any other modules you want to import directly from node_modules
    ],
  },
}); */


// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [],
});
